import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.EasyContract.app',
  appName: 'EasyContract',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
